kind: Deploy
type: kubernetes
name: iceberg-rest-deploy
description: K8s Deploy Iceberg REST
disabled: true
spec:
  manifestTemplates:
    - ./manifests/iceberg-rest.yaml.tpl
dependencies:
  - deploy.secrets
  - deploy.iceberg-database
environments:
  - local
  - remote